<form method="post" action="{{ path('app_back_city_delete', {'id': city.id}) }}" onsubmit="return confirm('Etes vous sur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ city.id) }}">
    <button class="btn btn-danger mt-1">Supprimer</button>
</form>
